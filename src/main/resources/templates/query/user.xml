<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.user.login.dao.UserDAO">
<!-- 
	<resultMap type="user" id="userResultMap">
		<id column="user_id" property="userId"/> 
		<result column="user_no" property="userNo"/> 
        <result column="user_pw" property="userPw"/>
        <result column="user_name" property="userName"/>
        <result column="user_email" property="userEmail"/>
        <result column="user_phone" property="userPhone"/>
        <result column="user_birth" property="userBirth"/>
        <result column="user_type" property="userType"/>
        <result column="user_date" property="userDate"/>
	</resultMap>
  -->
	<select id="login" parameterType="user" resultType="user">
      SELECT   user_no userNo, user_id userId, user_pw userPw, user_name userName, user_email userEmail, 
       user_phone userPhone, user_birth userBirth, user_type userType, user_date userDate
      FROM   USERS
      WHERE user_id = #{userId} and user_pw = #{userPw}
	</select>

	<insert id="signUp" parameterType="user">
	    /* 회원가입 */
	    INSERT INTO USERS(user_no, user_id, user_pw, user_email, user_phone, user_birth, user_type, user_date)
	    VALUES (users_seq.nextval, #{userId}, #{userPw}, #{userEmail}, #{userPhone}, #{userBirth},'일반회원',sysdate)
	</insert>
	
	<select id="idChk" parameterType="user" resultType="int">
		/* 아이디중복확인 */
		SELECT count(*)
		FROM USERS
		WHERE user_id=#{userId}
	</select>

	<update id="myPageUpdate" parameterType="user">
		/*정보 수정 */
		UPDATE USERS  
		SET	user_pw=#{userPw}, user_phone=#{userPhone}
		WHERE user_pw=#{userPw}
	</update>
<!--	
	<delete id="myPageDelete" parameterType="user" >
		/* 회원 탈퇴 */
		DELETE FROM USERS
		WHERE user_id=#{userId} and user_pw=#{userPw}
	</delete>
  -->
	
</mapper>